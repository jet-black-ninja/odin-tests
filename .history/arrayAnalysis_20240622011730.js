function arrayAnalysis(array){
    let object={};
    array.sort();
    let sum=0;
    let max=array[array.length-1];
    let min=array[0];
    for(let i=0;i<array.length;i++){
        sum+=array[i];
    }
}